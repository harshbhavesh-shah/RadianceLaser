// Shared between server-only code (lib/gmail/client.ts) and client
// components (components/admin/EmailInboxClient.tsx) — deliberately not
// "server-only" itself, since it's just a public address, not a secret.
export const RADIANCE_EMAIL_ADDRESS = "admin@radiancelaser.in";
